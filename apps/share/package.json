{
  "name": "@wellorbetter/share",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173",
    "build": "tsc --noEmit && vite build",
    "// deploy": "build first — wrangler deploy uploads whatever ./dist happens to hold, with no dependency on the source tree. Deploying without building ships the previous build: it succeeds, reports the uploaded asset count, and silently serves stale files. That is how the favicon and the font were declared, committed, merged, deployed, and still 404ing (PR #22).",
    "deploy": "npm run build && wrangler deploy",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@wellorbetter/design": "*",
    "@wellorbetter/shared": "*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-masonry-css": "^1.0.16"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^7.0.1",
    "@testing-library/react": "^16.3.2",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "jsdom": "^29.1.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vitest": "^4.1.10"
  }
}
